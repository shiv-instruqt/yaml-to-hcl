# Your job: rewrite the container from config.yml as a 2.0 HCL container resource.
# The legacy definition lives in /root/work/config.yml for reference.
#
# Delete the TODO below and replace it with a real container resource.
#
# TODO: resource "container" "webserver" { ... }
